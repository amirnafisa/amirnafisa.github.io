class TimeTravel extends React.Component {
    render () {
        return (
            <div>
                Demonstration Video and some description should come here.
            </div>
        );
    }
}

ReactDOM.render(<TimeTravel/>, document.querySelector("#timetravel"));